class Entry {
    let entry: Int
    let km: Int
    let gas: Double
    let type: Int
    
    init(entry: Int, km: Int, gas: Double, type: Int) {
        self.entry = entry
        self.gas = gas
        self.km = km
        self.type = type
    }
}
